program Pomiar506;

uses
  Forms,
  KonfiguracjaCOM in 'KonfiguracjaCOM.pas' {MainForm},
  OpenCOM1_3 in 'OpenCOM1_3.pas' {SecondForm1},
  DihgitalEkran in 'DihgitalEkran.pas' {SecondForm3},
  GraphEkran in 'GraphEkran.pas' {SecondForm2},
  Timer in 'Timer.pas' {SecondFormTime};

{$R *.RES}

begin
  Application.Initialize;
  Application.Title := 'Pomiar 506';
  Application.CreateForm(TMainForm, MainForm);
  Application.CreateForm(TSecondForm1, SecondForm1);
  Application.CreateForm(TSecondForm3, SecondForm3);
  Application.CreateForm(TSecondForm2, SecondForm2);
  Application.CreateForm(TSecondFormTime, SecondFormTime);
  Application.Run;
end.
